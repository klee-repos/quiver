# Secrets
.env

# Per-user config — your real watchlist / mode (dry_run) / risk caps. Kept private so
# a public repo never exposes your live posture. Copy config.yaml.example -> config.yaml.
config.yaml

# Runtime state & logs
state/
logs/
KILL

# Editor / local
.vscode/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.gstack/

# Deploy secrets / local state (only the *.example + IaC are tracked)
deploy/runner/runner.env
deploy/runner/mcp.json
deploy/terraform/terraform.tfvars
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/.terraform/
deploy/terraform/.terraform.lock.hcl
.claude/
