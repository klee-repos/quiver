# Secrets
.env

# Per-user config + strategy book — your real mode (dry_run) / risk caps and your macro
# allocations + the derived trading universe. Kept private so a public repo never exposes
# your live posture. Copy the *.example templates to start; on the box they're pulled from
# SSM (see DEPLOY.md).
config.yaml
strategy.yaml

# Runtime state & logs
state/
logs/
KILL

# Editor / local
.vscode/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.gstack/

# Deploy secrets / local state (only the *.example + IaC are tracked)
deploy/runner/runner.env
deploy/runner/mcp.json
deploy/terraform/terraform.tfvars
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/.terraform/
deploy/terraform/.terraform.lock.hcl
.claude/
